# ------------------------------------
# DATABASE CONNECTION (PostgreSQL)
# ------------------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=postgres

# ------------------------------------
# JPA/Hibernate Settings (Optional but helpful)
# ------------------------------------
# Show the generated SQL in the console (helpful for debugging)
spring.jpa.show-sql=true 
# Control database schema generation (crucial for local development)
spring.jpa.hibernate.ddl-auto=update

# finnhub data provide
baseUrl=https://finnhub.io/api/v1
finnhub.api.token=d42eropr01qorler208gd42eropr01qorler2090
